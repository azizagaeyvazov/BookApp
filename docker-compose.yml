version: '3'

services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_DB: Book
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234

  your_app:
    build: .
    depends_on:
      - postgres
    environment:
      DATABASE_URL: postgres://postgres:1234@postgres:5432/Book
    ports:
      - "8088:8080"